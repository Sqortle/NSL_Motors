spring.application.name=NSLMotors
# Server Port
server.port=8080

# --- Veritaban? Ba?lant?s? (PostgreSQL Örne?i) ---
spring.datasource.url=jdbc:postgresql://localhost:5432/araba_yazilim_db
spring.datasource.username=postgres
spring.datasource.password=sizin_sifreniz

# --- JPA/Hibernate Ayarlar? ---
# ddl-auto: update (mevcut tablolar? günceller), create (her seferinde s?f?rdan olu?turur)
# Geli?tirme a?amas?nda 'update' önerilir.
spring.jpa.hibernate.ddl-auto=update
# Konsolda SQL sorgular?n? görmek için
spring.jpa.show-sql=true

# Hibernate'in SQL dialektini belirtin
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- Mail Do?rulama ?çin (Simülasyon veya Gerçek Servis Ayarlar?) ---
spring.mail.host=smtp.example.com
spring.mail.port=587
# ... di?er mail ayarlar? buraya gelecek
